<form method="post" action="{{ path('estado_civil_delete', {'id': estado_civil.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ estado_civil.id) }}">
    <button class="btn">Delete</button>
</form>
