<form method="post" action="{{ path('issue_report_delete', {'id': issue_report.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ issue_report.id) }}">
    <button class="btn">Delete</button>
</form>
