{% extends 'base.html.twig' %}

{% block title %}TipoDocumento{% endblock %}

{% block body %}
    <h1>TipoDocumento</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipo_documento.id }}</td>
            </tr>
            <tr>
                <th>TipoDocumento</th>
                <td>{{ tipo_documento.tipoDocumento }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tipo_documento_index') }}">back to list</a>

    <a href="{{ path('tipo_documento_edit', {'id': tipo_documento.id}) }}">edit</a>

    {{ include('tipo_documento/_delete_form.html.twig') }}
{% endblock %}
