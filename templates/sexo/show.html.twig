{% extends 'base.html.twig' %}

{% block title %}Sexo{% endblock %}

{% block body %}
    <h1>Sexo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sexo.id }}</td>
            </tr>
            <tr>
                <th>Sexo</th>
                <td>{{ sexo.sexo }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ sexo.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sexo_index') }}">back to list</a>

    <a href="{{ path('sexo_edit', {'id': sexo.id}) }}">edit</a>

    {{ include('sexo/_delete_form.html.twig') }}
{% endblock %}
