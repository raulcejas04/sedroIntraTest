<form method="post" action="{{ path('sexo_delete', {'id': sexo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sexo.id) }}">
    <button class="btn">Delete</button>
</form>
