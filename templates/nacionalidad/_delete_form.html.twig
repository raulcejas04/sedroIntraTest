<form method="post" action="{{ path('nacionalidad_delete', {'id': nacionalidad.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nacionalidad.id) }}">
    <button class="btn">Delete</button>
</form>
