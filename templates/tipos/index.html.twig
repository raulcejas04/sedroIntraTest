{% extends 'base.html.twig' %}

{% block title %}TipoCuitCuil index{% endblock %}

{% block body %}
    <h1>TipoCuitCuil index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>TipoCuitCuil</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tipo_cuit_cuil in tipo_cuit_cuils %}
            <tr>
                <td>{{ tipo_cuit_cuil.id }}</td>
                <td>{{ tipo_cuit_cuil.tipoCuitCuil }}</td>
                <td>
                    <a href="{{ path('tipos_show', {'id': tipo_cuit_cuil.id}) }}">show</a>
                    <a href="{{ path('tipos_edit', {'id': tipo_cuit_cuil.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('tipos_new') }}">Create new</a>
{% endblock %}
