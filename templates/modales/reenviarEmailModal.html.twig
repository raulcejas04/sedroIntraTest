  {{form_start(formReenviarCorreo,{'action':path('solicitud_reenviarEmail',{'hash':solicitud.hash})})}}
                    <div class="modal fade" id="reenviarEmailModal" tabindex="-1" aria-labelledby="reenviarEmailModalLabel" aria-hidden="true">
                    <div class="modal-dialog">
                        <div class="modal-content">
                        <div class="modal-header">
                            <h5 class="modal-title" id="exampleModalLabel">Reenviar Email</h5>
                           {#  <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button> #}
                        </div>
                        <div class="modal-body">
                                {{form_label(formReenviarCorreo.mail)}}
                                {{form_widget(formReenviarCorreo.mail)}}
                                {{form_errors(formReenviarCorreo.mail)}}
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cerrar</button>
                            <button type="submit" class="modal-submit btn btn-primary">Aceptar</button>
                        </div>
                        </div>
                    </div>
                    </div>
{{form_end(formReenviarCorreo)}}