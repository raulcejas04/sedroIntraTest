{% extends 'base.html.twig' %}

{% block title %}GrupoKeycloak{% endblock %}

{% block body %}
    <h1>GrupoKeycloak</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ grupo_keycloak.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ grupo_keycloak.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('grupo_keycloak_index') }}">back to list</a>

    <a href="{{ path('grupo_keycloak_edit', {'id': grupo_keycloak.id}) }}">edit</a>

    {{ include('grupo_keycloak/_delete_form.html.twig') }}
{% endblock %}
