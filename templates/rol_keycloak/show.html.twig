{% extends 'base.html.twig' %}

{% block title %}RolKeycloak{% endblock %}

{% block body %}
    <h1>RolKeycloak</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rol_keycloak.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ rol_keycloak.nombre }}</td>
            </tr>
            <tr>
                <th>RoleSymfony</th>
                <td>{{ rol_keycloak.roleSymfony }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ rol_keycloak.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rol_keycloak_index') }}">back to list</a>

    <a href="{{ path('rol_keycloak_edit', {'id': rol_keycloak.id}) }}">edit</a>

    {{ include('rol_keycloak/_delete_form.html.twig') }}
{% endblock %}
